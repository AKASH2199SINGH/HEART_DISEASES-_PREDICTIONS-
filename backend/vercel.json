{
  "version": 2,
  "builds": [
    { "src": "backend/main.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "backend/main.py" }
  ],
  "devCommand": "uvicorn backend.main:app --host 0.0.0.0 --port 3000"
}
